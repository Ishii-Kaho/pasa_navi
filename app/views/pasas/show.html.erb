<%= stylesheet_link_tag 'pasas', media: 'all', 'data-turbolinks-track': 'reload' %>
<%= render 'layouts/header' %>
<div class="col-md-10 m-5">
  <div class="col-md-3">
    <h2 class="mt-3"><%= @pasa.facility %></h2>
  </div>
  <!--<div class="raty">-->
  <!--  <div class="raty-cleanliness">PA/SA清潔度</div>-->
  <!--  <div class="raty-congestion">PA/SA混雑度</div>-->
  <!--  <div class="raty-satisfaction">PA/SA満足度</div>-->

  <!--<script>-->
  <!--  $('#star-rate-cleanliness<%#= @pasa.id %>').raty({-->
  <!--    size: 36,-->
  <!--    starOff:  '<%#= asset_path('star-off.png') %>',-->
  <!--    starOn : '<%#= asset_path('star-on.png') %>',-->
  <!--    starHalf: '<%#= asset_path('star-half.png') %>',-->
  <!--    half: true,-->
  <!--    readOnly: true,-->
  <!--    score: <%#= @posts.cleanliness.average(:cleanliness).to_f.round(1) %>,-->
  <!--  });-->
  <!--  </script> -->

  <!--  javascript:-->
  <!--$('#star-rate-#{food.id}').raty({-->
  <!--  size: 36,-->
  <!--  starOff: "#{asset_path('star-off.png')}",-->
  <!--  starOn: "#{asset_path('star-on.png')}",-->
  <!--  starHalf: "#{asset_path('star-half.png')}",-->
  <!--  half: true,-->
  <!--  readOnly: true,-->
  <!--  score: "#{food.reviews.average(:rate).to_f.round(1)}",-->
  <!--  //注目ポイント↑ 平均点を算出し、round関数で切り上げ-->
  <!--});-->
  </div>

  <div class="post">
    <div class="row">
      <% @posts.each do |post| %>
        <div class="col-md-3 mt-5">
          <div class="one-post">
            <h4 class="post-title">
              <%= post.title %>
            </h4>
            <div class="image">
              <%= link_to post_path(post.id) do %>
                <%= attachment_image_tag post, :image, format: 'jpeg', fallback: "no_image.jpg", size:'240x160', class: "image" %>
              <% end %>
            </div>
            <div class="profile m-3">
              <%= attachment_image_tag @post, :profile_image, format: 'jpeg', fallback: "no_image.jpg", size:'50x50', class: "image" %><%= post.user.name %>
            </div>
            <div class="favorites">
                ♥<%= post.favorites.count %> いいね
            </div>
            <div class="created_at">
                <%= post.created_at %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>